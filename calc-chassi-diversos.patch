*** a/frontend/src/utils/calcularOrcamento.js
--- b/frontend/src/utils/calcularOrcamento.js
@@
   // chassis
   incluirChassi = false,
   chassiSelecionado = null,
 
+  // diversos (opcional)
+  diversoSelecionado = null,
 
 }) {
@@
   if (temPP) {
@@
   }
 
-  // === chassi (Tela) ===
-    let chassiInfo = null;
-    if (incluirChassi && chassiSelecionado?.preco) {
-      const precoML = Number(chassiSelecionado.preco || chassiSelecionado.preco_ml || 0);
-      const custo = precoML * perimetro_m * (Number(quantidade) || 1);
-      custos.chassi = custo;
-      chassiInfo = {
-        mm: /5mm/i.test(chassiSelecionado.nome || '') ? '5mm' : (/3mm/i.test(chassiSelecionado.nome || '') ? '3mm' : ''),
-        nome: chassiSelecionado.nome || 'Chassi',
-        precoML,
-        ml: perimetro_m,
-      };
-    }
+  // === Chassi (Tela) — custo por metro linear do perímetro interno ===
+  const precoChassiML = num(pick(chassiSelecionado || {}, ['preco_ml','preco','valor_ml','valor']), 0);
+  const custoChassiUnit = (incluirChassi && precoChassiML > 0)
+    ? perimetroInternoM * precoChassiML
+    : 0;
+  const chassiInfo = (incluirChassi && precoChassiML > 0)
+    ? {
+        mm: (/5mm/i.test(chassiSelecionado?.nome || '')) ? '5mm' :
+            ((/3mm/i.test(chassiSelecionado?.nome || '')) ? '3mm' : ''),
+        nome: chassiSelecionado?.nome || 'Chassi',
+        precoML: precoChassiML,
+        ml: perimetroInternoM,
+      }
+    : null;
+
+  // === Diversos (opcional) — unitário / ml / m2 ===
+  let custoDiversoUnit = 0;
+  let diversoInfo = { aplicado: false };
+  if (diversoSelecionado) {
+    const unidade = String(diversoSelecionado.unidade || '').toLowerCase();
+    const precoBase = num(
+      pick(diversoSelecionado, ['preco','valor','preco_ml','valor_ml','preco_m2','valor_m2']),
+      0
+    );
+    if (precoBase > 0) {
+      if (unidade === 'ml')       custoDiversoUnit = precoBase * perimetroInternoM;
+      else if (unidade === 'm2')  custoDiversoUnit = precoBase * areaObraM2;
+      else                        custoDiversoUnit = precoBase; // unitário / default
+      diversoInfo = {
+        aplicado: true,
+        nome: diversoSelecionado.nome || 'Serviço',
+        unidade,
+        faixa: diversoSelecionado.faixa_aplicacao || null,
+        precoBase,
+      };
+    }
+  }
@@
   const subtotalMateriaisUnit =
     custoMoldurasTotal +
     custoBagueteInterna +
     custoVidro +
     custoFundo +
     custoFundoExtra +
     custoPP +
     custoAberturasExtras +
     custoImpressao +
+    custoChassiUnit +
+    custoDiversoUnit +
     camisaObjetoExtra +
     (aplicaReforco ? valorReforco : 0);
@@
     reforcoInfo,
+    chassiInfo,
+    diversoInfo,
     mensagemAviso: mensagemAviso || null,
     modoCobrancaPassepartout,
@@
       camisaObjetoExtra,
 
-      chassi: 0,
+      chassi: custoChassiUnit,
+      diversos: custoDiversoUnit,
 
       reforco: valorReforco,
